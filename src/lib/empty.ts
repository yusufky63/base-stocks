/**
 * Empty module used by `turbopack.resolveAlias` to satisfy optional peer dependencies that
 * transitive wallet SDKs import dynamically (x402 / Solana payment helpers we never call).
 */
export {};
